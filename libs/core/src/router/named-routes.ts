export class NamedRoutes {
  // stored routes

  getRoute(key: string, params?: Record<string, unknown>): unknown {
    // Get the route from storage
    return undefined;
  }

  register(key: string, value: unknown): void {
    // store a new route
  }
}

// Some route definition
// Needs to replace named params in order to work
export class NamedRoute {

}
