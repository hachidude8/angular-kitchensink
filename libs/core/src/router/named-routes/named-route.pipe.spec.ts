import { NamedRoutePipe } from './named-route.pipe';

describe('NamedRoutePipe', () => {
  it('create an instance', () => {
    const pipe = new NamedRoutePipe();
    expect(pipe).toBeTruthy();
  });
});
